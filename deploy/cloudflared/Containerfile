FROM cloudflare/cloudflared:latest
# El token se lee del entorno (EnvironmentFile del quadlet, chmod 600):
# nunca va en la imagen ni en el repo.
CMD ["sh", "-c", "cloudflared tunnel --no-autoupdate run --token \"$TUNNEL_TOKEN\""]
